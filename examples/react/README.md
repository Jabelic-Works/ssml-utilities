# SSML Utilities React Example

`examples/react` は `@ssml-utilities/editor-react` の利用例とローカル動作確認用のサンプルです。

## 開発

```bash
pnpm install
pnpm --filter react dev
```

## ビルド

```bash
pnpm --filter react build
```

## プレビュー

```bash
pnpm --filter react preview
```
